const system = {
    refreshTime: 9 * 60 * 1000,
    endpointURL: process.env.EXPO_PUBLIC_APPWRITE_ENDPOINT,
    googleMapAPIKey: process.env.EXPO_PUBLIC_GOOGLE_MAPS_API_KEY,
}

export default system;
